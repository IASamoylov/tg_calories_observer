name: Rollback

on:
  workflow_dispatch:
    inputs:
      tag:
        description: "release tag"
        type: string
        required: true

jobs:
  tag:
    name: Check tag
    runs-on: ubuntu-latest
    steps:
      - run: echo "🎉 release ref ${{ inputs.tag }}"
      - name: Checkout
        uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - run: git tag -l | grep -e "v"

  publish:
    name: Publish
    needs: tag
    uses: ./.github/workflows/publish.yaml
    with:
      ref: ${{ inputs.tag }}
    secrets: inherit

  deployment:
    name: Deploy
    needs:
      - publish
    uses: ./.github/workflows/deploy.yaml
    with:
      target: "Production"
      tag: ${{ needs.publish.outputs.GITHUB_SHA_SHORT }}
      revision-core-fraction: 25
      container-name: ${{ needs.publish.outputs.APP_NAME }}
    secrets: inherit
